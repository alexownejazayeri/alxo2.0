nav.topnav
  .topnav__container
    include ./logo.pug
    .topnav__hamburger
      button.topnav__link
        include ./hamburger.pug
    if !userDevice.isMobile
      .topnav__location Hello from Oakland, CA 🌲
    if userDevice.isMobile
      include ./alxo-icon.pug
